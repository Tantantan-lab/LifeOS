# LifeOS — local Postgres connection template.
# Copy to .env.local. SERVER-ONLY: never prefix with NEXT_PUBLIC_.
# The database binds to 127.0.0.1 only (see compose.yaml).

DATABASE_URL=postgresql://lifeos:lifeos@127.0.0.1:5432/lifeos
