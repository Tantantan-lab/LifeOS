# @lifeos/ui

Shared design system package. Arrives in **M4**, extracted from `apps/web`.

Will contain: design tokens (`tokens.css`), primitives (`Panel`, `StatNumber`, `MetricDelta`, `ProgressBar`, `DomainChip`, `Sparkline`), and the heatmap components — everything currently living in `apps/web/src/styles` + `apps/web/src/components`.
